<%= form_with(model: compte, local: true) do |form| %>
  <% if compte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(compte.errors.count, "error") %> prohibited this compte from being saved:</h2>

      <ul>
      <% compte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hiéarchie %>
<%= text_field_tag :hiéarchie, nil, placeholder: 'Student or Teacher' %>
  </div>

  <div class="field">
    <%= form.label :identifiant %>
    <%= form.text_field :identifiant %>
  </div>

  <div class="field">
    <%= form.label :mdp %>
    <%= form.text_field :mdp %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
